# Hugging Face Space runtime.
# Key-free retrieval remains available; Gemini generation is optional at runtime.
gradio==4.44.1
numpy>=1.26,<3
pypdf>=4.2,<6
sentence-transformers>=2.7,<4

# Reusable async Gemini service used by gemini_adapter.py.
furqunali-gemini-chatbot @ git+https://github.com/furqunali/gemini-chatbot.git@feat/package-for-rag-integration

# Optional legacy OpenAI generation.
# openai>=1.30
